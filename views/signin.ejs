<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>APP 16-4</title>
  </head>
  <body>
    <% if (!locals.signedIn) { %>
    <form method="POST" action="/signin">
      <input
        type="text"
        name="login"
        placeholder="Login"
        value="<%= login %>"
      />
      <br />
      <input
        type="password"
        name="password"
        placeholder="Password"
        value="<%= password %>"
      />
      <br />
      <input type="checkbox" name="save" value="checkbox" />Save
      <!-- <%= Save %> -->
      <br /><br />
      <button>OK</button>
    </form>
    <% } %>
    <div id="result">
      <% if (login && password) { %> Hello , <%= login %> <br />
      <h4>You are logged in.</h4>
      <a href="/signout">Log out</a>
      <% } else if (locals.valid == false) { %>
      <h4 style="color: red">login or password is invalid.</h4>
      <% } %>
    </div>
  </body>
</html>
